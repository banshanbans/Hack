# Asset slots

- `video/`：授权的产品录屏；
- `audio/`：授权的主音乐；
- `ui/`：产品 UI 截图；
- `product/`：设备和 Hero Shot；
- `sfx/`：授权的转场与节拍音效。

所有 Remotion `asset` 路径均相对 `public/`，例如 `assets/ui/result-ui.png`。请不要放入真实用户家庭影像或未确认授权的音频/视频。
