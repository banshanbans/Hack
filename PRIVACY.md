# 隐私与数据处理

## iOS App

- 相机和深度信息只用于本次房间辅助筛查；
- 开启 RTC 视频顾问后，扫描期间将相机画面实时发送给火山 RTC 视觉顾问；不配置云端录制，视频和原始音频不持久化；
- App 需要在线加载 H5，离线时只显示重试，不回退历史原生旅程；
- 只有通过质量和变化门控的稳定帧才会额外作为显式检查图片发送，用于生成可追溯的结构化临时建议；
- 本地稳定帧使用最多 8 张且不超过 24MB 的内存缓存，扫描结束、取消或超时后释放；
- assessment token 仅存在于当次原生操作内存，不写入 URL、日志、UserDefaults 或 Keychain；
- 取消扫描不上传临时帧；完成后仅保留上传成功的代表帧，临时 AR 锚点会立即清理；
- App 不包含第三方模型永久密钥；
- 日志不记录原始图像、家庭地址或问题描述全文；
- 无可靠深度或射线交点时，不创建世界锚点。

## H5

- 正式评估只接收用户选择的照片；
- 不提供本地视频文件上传、自动抽帧或视频关键帧正式识别；若用户从视频自行截图，该截图按普通照片处理；
- 实时相机只在用户主动授权后工作；开启 RTC 视频顾问时同一摄像头 Track 用于本地预览、实时发送、质量门控和代表帧截取，离开页面时停止；
- RTC 视频、显式检查图片和临时帧不持久化，也不直接进入评分或报告；只有用户结束扫描后选出的代表画面按正式媒体保存；
- 浏览器不保存模型厂商永久密钥。
- App 内 WKWebView 与外部 Safari 各自保存独立 assessment，不实现账号、注册、跨设备或两者之间的状态同步。

## AI 适老顾问

- 语音只在用户点击麦克风后开启，90 秒无操作自动停听，单次 RTC 任务最长 15 分钟；
- 不保存原始音频；只保存本次 assessment 内的文字转写、结构化卡片和关联 ID，房间历史最多 200 个 turn；
- 删除 assessment 时对话记录级联删除；结束 RTC 任务只停止语音，不会单独删除文字历史；
- 客户端只获得短期房间 Token 与一次性事件 Token；RTC AppKey 与火山 OpenAPI 凭据只存在服务端。
- Function Calling 回调使用独立签名，并校验 App、RTC 房间、任务、assessment、房间和临时检查帧归属；工具日志不保存图片、原始音频或完整档案。

## 本地开发服务

- v2 使用 SQLite 保存 assessment 和结构化状态，规范化照片保存在受控媒体目录；
- 用户删除照片或 assessment 时同步删除在线副本；备份按配置的保留周期自然淘汰；
- 分享 token 使用不可猜测的随机值，默认 24 小时过期；
- 进程重启后，仅进程内临时相机帧、请求锁和短期任务上下文失效；SQLite 与媒体目录中的 assessment 数据按用户删除或配置的保留策略处理；
- 对外部署必须使用 HTTPS、持久化过期策略、访问控制和合规的数据删除机制。
