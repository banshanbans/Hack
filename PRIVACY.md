# 隐私与数据处理

## iOS App

- 相机和深度信息只用于本次房间辅助筛查；
- 默认不上传连续视频；
- 远端能力关闭时，扫描和本地报告仍可完成；
- 开启远端能力后，每次只发送受控关键帧；
- 历史帧上下文使用容量为 4 的内存缓存，响应、取消或超时后释放；
- App 不包含第三方模型永久密钥；
- 日志不记录原始图像、家庭地址或问题描述全文；
- 无可靠深度或射线交点时，不创建世界锚点。

## 本地开发服务

- 只在内存中保存 Session、结构化问题状态和短期分享 token；
- 不保存上传图片；
- 默认返回空分析结果；
- 分享 token 使用不可猜测的随机值，默认 24 小时过期；
- 进程重启后所有内存数据失效；
- 对外部署必须使用 HTTPS、持久化过期策略、访问控制和合规的数据删除机制。
